##PROJECT REALIZED WITH NODE.JS AND EXPRESS
        // if(entity == "Appointment"){
        //     if(attr === "doctor"){
        //         return await appointments.filter((data) => data.id_doctor == id)
        //     }
        //     if(attr === "patient"){
        //         return await appointments.filter((data) => data.id_patient == id)
        //     }
        // }